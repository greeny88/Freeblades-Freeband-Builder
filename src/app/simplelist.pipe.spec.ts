import { SimpleListPipe } from './simplelist.pipe';

describe('SimpleListPipe', () => {
    it('create an instance', () => {
        const pipe = new SimpleListPipe();
        expect(pipe).toBeTruthy();
    });
});
